spring:
  application:
    name: server-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/wenlongup/spring-cloud.git
          default-label: master
          search-paths: config
server:
  port: 8889